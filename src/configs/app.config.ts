import type { Metadata } from "next";
import { APP_DESCRIPTION, APP_NAME } from "@/common/constants/app.constant";

export const appMetadata: Metadata = {
  title: APP_NAME,
  description: APP_DESCRIPTION,
};
